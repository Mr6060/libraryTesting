#include "Game.hpp"


int main()
{
    MyGame::start();

    /*MyGame::Game game;
    
    while(!game.GetWindow() -> isDone()){
        game.HandleInput();
        game.Update();
        game.Render();
        game.RestartClock();
    }
    */
    return 0;
}